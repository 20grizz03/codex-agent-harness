interface:
  display_name: "Delivery Writing"
  short_description: "Текст для человека: от первого черновика до публикации"
  default_prompt: "Используй $delivery-writing до первого черновика: примени правила языка и нужного формата, не публикуя текст без разрешения."
policy:
  allow_implicit_invocation: true
